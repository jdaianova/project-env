image: Ubuntu1804  # образ для сборки

stack: node 16  # окружение

branches:
  only:
    - master  # ветка git

cache: node_modules  # кеширование

install:
  - npm install  # команда установки зависимостей

build: off  # отключаем встроенную в appveyor систему сборки

build_script:
  - npm run build   # команда сборки

test_script:
  - npm run lint && npm test  # скрипт тестирования

deploy:
  - provider: GitHub
    auth_token:
      secure: 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
#    artifact: /.*\.nupkg/            # upload all NuGet packages to release assets
    draft: false
    prerelease: false
    on:
      branch: gh-pages                 # release from master branch only
      appveyor_repo_tag: true        # deploy on tag push only
#deploy_script:  # скрипт развёртывания
#  - git config --global credential.helper store
#  - git config --global user.name jdaianova
#  - git config --global user.email jdaianova@gmail.com
#  - echo "https://$GITHUB_TOKEN:x-oauth-basic@github.com" > "$HOME/.git-credentials"
#  - npx push-dir --dir=dist --branch=gh-pages --force --verbose

